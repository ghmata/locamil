# Configurações da Aplicação Flask
# Copie este arquivo para .env e preencha com seus valores reais

# Chave secreta do Flask (IMPORTANTE: Gere uma chave única e segura)
# Para gerar uma chave segura, execute em Python:
# import secrets
# print(secrets.token_hex(32))
SECRET_KEY=sua-chave-secreta-aqui-mude-em-producao

# Configuração do Banco de Dados
# Para SQLite (padrão): sqlite:///locadora.db
# Para PostgreSQL: postgresql://usuario:senha@localhost/locadora
# Para MySQL: mysql://usuario:senha@localhost/locadora
DATABASE_URI=sqlite:///locadora.db

# Modo Debug (True para desenvolvimento, False para produção)
FLASK_DEBUG=True

# Host e Porta
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
